---
db_vm_type: 'concourse-db'   # the VM type for Postgres DB
db_persistent_disk_type: 'concourse-db'  # the disk type for persistence
web_vm_type: 'concourse-web'  # the VM type for Concourse web VM
worker_vm_type: 'concourse-workers'  # the VM type for worker VMs
db_ip: '10.0.0.213'  # the IP address reserved for the Postgres DB
web_ip: '10.0.0.212'  # the IP address of Concourse web VM
deployment_name: concourse
network_name: concourse
external_host: ci.lab.khadilkar.net
external_url: https://ci.lab.khadilkar.net
main_team_oauth_users: ["concourse"]
